# One Line Labyrinth

A one-line path puzzle built with Flutter for iOS, Android, and web.

## Local Web Build

```sh
flutter build web --release --base-href /OneLineLabyrinth/
```

GitHub Pages deploys from `.github/workflows/deploy-pages.yml`.
